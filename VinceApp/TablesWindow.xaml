<Window x:Class="VinceApp.TablesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:VinceApp"
        Title="اختيار الطاولة" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized" FlowDirection="RightToLeft" WindowStyle="None"
        Background="#F2F5F8" ContentRendered="Window_ContentRendered">
    <Window.Resources>

        <DropShadowEffect x:Key="CardShadow" BlurRadius="12" ShadowDepth="2" Opacity="0.1" Color="#000000"/>

        <SolidColorBrush x:Key="BgBrush" Color="#F2F5F8"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TitleBrush" Color="#37474F"/>
        <Style x:Key="TouchButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="58"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="6,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}"
                                BorderBrush="#00FFFFFF"
                                BorderThickness="0"
                                SnapsToDevicePixels="True">

                            <Border.Effect>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.15" Color="#000000"/>
                            </Border.Effect>

                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CFD8DC"/>
                                <Setter Property="Foreground" Value="#90A4AE"/>
                                <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

    </Window.Resources>

    <Grid Background="{StaticResource BgBrush}">
        <Grid Margin="22">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="86"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#FFFFFF"
                    CornerRadius="16"
                    BorderBrush="{StaticResource BorderBrushSoft}"
                    BorderThickness="1"
                    Padding="18,12"
                    Margin="0,0,0,16"
                    Effect="{StaticResource CardShadow}">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🍽️" FontSize="26" Margin="0,0,10,0"/>
                        <TextBlock Text="صالة الطعام" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TitleBrush}"/>
                        <TextBlock Text=" | اختيار الطاولة" FontSize="24" Foreground="#78909C" Margin="5,0,0,0"/>
                    </StackPanel>

                    <Border Grid.Column="1"
                            Background="#F5F5F5"
                            CornerRadius="8"
                            Padding="12,6"
                            VerticalAlignment="Center">
                        <local:DigitalClock HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#455A64"/>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Style="{StaticResource CardBorderStyle}"
                        Padding="18"
                        Margin="0,0,12,0">

                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  PanningMode="VerticalOnly">
                        <WrapPanel x:Name="TablesPanel"
                                   HorizontalAlignment="Center"
                                   Margin="6"/>
                    </ScrollViewer>
                </Border>

                <Border Grid.Column="1"
                        Background="#455A64"
                        CornerRadius="16"
                        BorderBrush="#546E7A"
                        BorderThickness="1"
                        Padding="14"
                        Effect="{StaticResource CardShadow}">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Background="#37474F"
                                CornerRadius="12"
                                Padding="12"
                                Margin="0,0,0,12"
                                BorderBrush="#546E7A"
                                BorderThickness="1">
                            <TextBlock Text="🛍️ سفري معلق"
                                       Foreground="White"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"/>
                        </Border>

                        <ScrollViewer Grid.Row="1"
                                      VerticalScrollBarVisibility="Auto"
                                      PanningMode="VerticalOnly">
                            <StackPanel x:Name="TakeawayPanel"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0,10,0,0">

                <Button x:Name="btnNewTakeaway"
                        Content="🛍️ طلب سفري جديد"
                        Width="230"
                        Background="#7E57C2" 
                        FontSize="20"
                        Style="{StaticResource TouchButtonStyle}"
                        Click="TakeawayButton_Click"/>

                <Button x:Name="btnRefresh"
                        Content="🔄 تحديث"
                        Width="150"
                        Background="#607D8B"
                        Style="{StaticResource TouchButtonStyle}"
                        Click="RefreshButton_Click"/>

                <Button x:Name="btnAdmin"
                        Content="👔 الإدارة"
                        Width="150"
                        Background="#607D8B"
                        Style="{StaticResource TouchButtonStyle}"
                        Click="OpenAdmin_Click"/>

                <Button x:Name="btnSettings"
                        Content="⚙️ الإعدادات"
                        Width="150"
                        Background="#607D8B"
                        Style="{StaticResource TouchButtonStyle}"
                        Click="SettingsButton_Click"/>

                <Button x:Name="btnExit"
                        Content="🚪 خروج" 
                        Width="130"
                        Background="#E57373"
                        Style="{StaticResource TouchButtonStyle}"
                        Click="ExitButton_Click"/>
            </StackPanel>

            <Grid x:Name="LoadingOverlay"
                  Grid.RowSpan="3"
                  Visibility="Collapsed"
                  Background="#99FFFFFF">
                <Border Background="White"
                        Width="440"
                        Padding="24"
                        CornerRadius="18"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Effect="{StaticResource CardShadow}">

                    <StackPanel>
                        <TextBlock Text="جاري التحميل..."
                                   FontSize="22"
                                   FontWeight="Bold"
                                   Foreground="#37474F"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,12"/>

                        <ProgressBar IsIndeterminate="True"
                                     Height="6"
                                     Foreground="#7E57C2"
                                     BorderThickness="0"
                                     Background="#ECEFF1"
                                     Margin="0,0,0,12"/>

                        <TextBlock x:Name="txtLoadingHint"
                                   Text="يتم تجهيز الطاولات والطلبات"
                                   FontSize="14"
                                   Foreground="#78909C"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

        </Grid>

        <Grid x:Name="ToastOverlay" IsHitTestVisible="True" Panel.ZIndex="9999" Background="{x:Null}">
            <StackPanel x:Name="NotificationArea"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="20"
                        Width="360"/>
        </Grid>
    </Grid>
</Window>