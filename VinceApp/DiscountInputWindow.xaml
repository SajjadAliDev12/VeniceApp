<Window x:Class="VinceApp.DiscountInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدخال الخصم" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None"
        Background="White" FlowDirection="RightToLeft" BorderBrush="#D32F2F" BorderThickness="2">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="تحديد قيمة الخصم" HorizontalAlignment="Center" FontSize="22" FontWeight="Bold" Foreground="#5D4037" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="#FAFAFA" BorderBrush="#D32F2F" BorderThickness="1" CornerRadius="10" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="القيمة الحالية:" FontSize="14" Foreground="#777"/>
                <TextBlock x:Name="txtDisplay" Text="0" FontSize="36" FontWeight="Bold" Foreground="#D32F2F" HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtError" Text="" FontSize="14" Foreground="Red" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Button Content="7" Grid.Row="0" Grid.Column="0" Click="Num_Click"/>
            <Button Content="8" Grid.Row="0" Grid.Column="1" Click="Num_Click"/>
            <Button Content="9" Grid.Row="0" Grid.Column="2" Click="Num_Click"/>

            <Button Content="4" Grid.Row="1" Grid.Column="0" Click="Num_Click"/>
            <Button Content="5" Grid.Row="1" Grid.Column="1" Click="Num_Click"/>
            <Button Content="6" Grid.Row="1" Grid.Column="2" Click="Num_Click"/>

            <Button Content="1" Grid.Row="2" Grid.Column="0" Click="Num_Click"/>
            <Button Content="2" Grid.Row="2" Grid.Column="1" Click="Num_Click"/>
            <Button Content="3" Grid.Row="2" Grid.Column="2" Click="Num_Click"/>

            <Button Content="C" Grid.Row="3" Grid.Column="0" Foreground="Red" Click="Clear_Click"/>
            <Button Content="0" Grid.Row="3" Grid.Column="1" Click="Num_Click"/>
            <Button Content="⌫" Grid.Row="3" Grid.Column="2" FontSize="20" Click="Back_Click"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Content="تأكيد الخصم" Click="Confirm_Click" Width="160" Height="60" Background="#2E7D32" Foreground="White" BorderBrush="Transparent"/>
            <Button Content="إلغاء" Click="Cancel_Click" Width="100" Height="60" Background="#757575" Foreground="White" BorderBrush="Transparent"/>
        </StackPanel>
    </Grid>
</Window>