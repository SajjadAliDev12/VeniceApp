<Window x:Class="VinceApp.Toters_Feature.TotersOrderNumberInput"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VinceApp.Toters_Feature"
        mc:Ignorable="d" FlowDirection="RightToLeft" 
        WindowState="Normal" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Title="إدخال رقم طلب توترز" Height="260" Width="430">

    <Window.Resources>
        <!-- قوالب الألوان الموحدة لهوية التطبيق -->
        <SolidColorBrush x:Key="BorderSoft" Color="#CFD8DC"/>
        <SolidColorBrush x:Key="TextDark" Color="#37474F"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#7E57C2"/>

        <!-- ستايل حقل الإدخال الذكي المتجاوب مع الفوكس -->
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="46"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ستايل الأزرار اللمسية الموحد -->
        <Style x:Key="TouchButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- إضافة تأثير إسقاط الظل (DropShadow) لكي تصبح النافذة بارزة وواضحة جداً عند ظهورها فوق النوافذ الأخرى -->
    <Grid Margin="15">
        <Border Background="White" CornerRadius="18" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="15" Opacity="0.15"/>
            </Border.Effect>

            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- العنوان العلوي -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="🛵" FontSize="22" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="ادخل رقم الطلب الخاص بتوترز:" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource TextDark}" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- حقل الإدخال الرئيسي الممتد -->
                <TextBox Name="txtTotersOrderId" 
                         Grid.Row="1" 
                         Style="{StaticResource InputTextBoxStyle}" 
                         VerticalAlignment="Center" 
                         HorizontalAlignment="Stretch"
                         TextAlignment="Center"/>

                <!-- شريط الأزرار التفاعلية الموزعة بالتساوي -->
                <Grid Grid.Row="2" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <!-- مسافة فاصلة بين الزرين -->
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- زر موافق بالألوان الأساسية للنظام -->
                    <Button Content="موافق" 
                            Grid.Column="0" 
                            Background="{StaticResource PrimaryColor}" 
                            Style="{StaticResource TouchButtonStyle}" 
                            Click="BtnOk_Click" 
                            IsDefault="True"/>

                    <!-- زر إلغاء بالألوان التحذيرية المريحة للعين -->
                    <Button Content="إلغاء" 
                            Grid.Column="2" 
                            Background="#90A4AE" 
                            Style="{StaticResource TouchButtonStyle}" 
                            Click="Button_Click" 
                            IsCancel="True"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>