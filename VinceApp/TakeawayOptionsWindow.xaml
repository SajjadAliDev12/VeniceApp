<Window x:Class="VinceApp.TakeawayOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="خيارات الطلب" Height="380" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        FlowDirection="RightToLeft">

    <Window.Resources>

        <!-- زر رئيسي (Touch friendly) -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="58"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b"
                                Background="{TemplateBinding Background}"
                                CornerRadius="14"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="b" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="b" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- زر ثانوي (إغلاق) -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#EEF2F5"/>
            <Setter Property="Foreground" Value="#4E5A63"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

    </Window.Resources>

    <Border Background="White" CornerRadius="20" BorderBrush="#8D6E63" BorderThickness="2">
       
        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="خيارات الطلب الجاهز"
                           FontSize="22" FontWeight="Bold"
                           Foreground="#5D4037"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="اختر الإجراء المناسب للطلب السفري"
                           FontSize="13"
                           Foreground="#777"
                           HorizontalAlignment="Center"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">

                <Button x:Name="btnView"
        Content="👁️ عرض محتويات الفاتورة"
        Background="#3B74B6"
        Click="BtnView_Click"
        Style="{StaticResource PrimaryButtonStyle}"/>
                <Button x:Name="btnServe"
                        Content="➕ أضافة ملحق للطلب"
                        Click="btnAdd_Click"
                        Background="#2F7D6B"
                        
                        Style="{StaticResource PrimaryButtonStyle}"/>

                

                <Button x:Name="btnAdd"
                        Content="✅ تسليم للزبون (إخفاء)"
                        Background="#B55E4A"
                        Click="BtnServe_Click"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,0,0,0"/>

            </StackPanel>

            <!-- Footer -->
            <Button Grid.Row="2"
                    Content="إغلاق"
                    Click="BtnCancel_Click"
                    IsCancel="True"
                    Style="{StaticResource SecondaryButtonStyle}"/>
        </Grid>
    </Border>
</Window>
